export interface EventDto {
  actualInfo: string;
  country: string;
  eventName: string;
  forecastInfo: string;
  id: number | null; // Assuming 'id' can be a number or null
  previousInfo: string;
  sentiment: string;
  time: string;
  volatility: string;
  }
